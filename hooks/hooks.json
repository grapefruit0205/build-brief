{
  "description": "Gate supported local mutations until an explicitly invoked Click execution contract has been staged, explained plainly, approved unchanged, and passed, with user opt-out and optional strict mode.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^(Bash|apply_patch|Edit|Write)$",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${PLUGIN_ROOT}/hooks/click_gate.py\" pre-tool",
            "commandWindows": "py -3 \"%PLUGIN_ROOT%\\hooks\\click_gate.py\" pre-tool",
            "timeout": 2
          }
        ]
      }
    ]
  }
}
