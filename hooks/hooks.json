{
  "description": "Gate supported local mutations until an explicitly invoked Build Brief contract has been explained plainly and approved, with user opt-out and optional strict mode.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^(Bash|apply_patch|Edit|Write)$",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${PLUGIN_ROOT}/hooks/build_brief_gate.py\" pre-tool",
            "commandWindows": "py -3 \"%PLUGIN_ROOT%\\hooks\\build_brief_gate.py\" pre-tool",
            "timeout": 2
          }
        ]
      }
    ]
  }
}
