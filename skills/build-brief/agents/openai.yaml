interface:
  display_name: "Build Brief"
  short_description: "Gate edits behind the smallest sufficient design"
  default_prompt: "Use $build-brief to form the smallest sufficient Design Contract, pass the mutation gate, and execute this software request."
policy:
  allow_implicit_invocation: true
