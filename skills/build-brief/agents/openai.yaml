interface:
  display_name: "Build Brief"
  short_description: "Translate software intent into context-aware build commands"
  default_prompt: "Use $build-brief to complete an implementation-ready design contract before coding, then execute this request."
policy:
  allow_implicit_invocation: true
