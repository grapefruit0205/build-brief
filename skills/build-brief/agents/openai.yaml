interface:
  display_name: "Build Brief"
  short_description: "Gate software edits behind a proportional design contract"
  default_prompt: "Use $build-brief to form a proportional Design Contract, pass the mutation gate, and execute this software request."
policy:
  allow_implicit_invocation: true
