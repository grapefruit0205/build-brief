interface:
  display_name: "Build Brief"
  short_description: "Explicitly gate implementation behind sufficient design"
  default_prompt: "Use $build-brief to form the smallest sufficient Design Contract, arm and pass its mutation gate, and execute this software request."
policy:
  allow_implicit_invocation: false
