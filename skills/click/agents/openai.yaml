interface:
  display_name: "Click"
  short_description: "Approve once, then implement the contract in one shot"
  default_prompt: "Use $click to stage one compact contract with a verification scale, explain it plainly, wait for later-turn approval, then implement it in one shot with hardened inspect, mutate, and verify capabilities."
policy:
  allow_implicit_invocation: true
