interface:
  display_name: "Fix"
  short_description: "Translate a bug report into one approved repair"
  default_prompt: "Use $fix to translate this bug report into a compact repair contract with a recommended verification scale, explain it plainly, and ask once before fixing it."
policy:
  allow_implicit_invocation: false
