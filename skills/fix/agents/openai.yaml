interface:
  display_name: "Fix"
  short_description: "Translate a bug report into one approved repair"
  default_prompt: "Use $fix to stage one compact repair contract with a verification scale, explain it plainly, wait for later-turn approval, then repair it in one shot with hardened inspect, mutate, and verify capabilities."
policy:
  allow_implicit_invocation: false
