interface:
  display_name: "Fix"
  short_description: "Translate a bug report into one approved repair"
  default_prompt: "Use $fix to translate this bug report into a compact repair contract with one verification scale, explain it plainly, ask once, then repair it with structured inspect, mutate, and verify capabilities."
policy:
  allow_implicit_invocation: false
